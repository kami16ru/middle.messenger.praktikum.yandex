<nav>
  <ul class="nav-drawer__menu">
    {{#each navList}}
      <li class="nav-drawer__menu-item">
        <a href="{{ this.href }}">
          <span class="mdi {{ this.icon }} h1 info"></span>
          <span class="nav-menu-title info" {{#unless collapsed}}hidden{{/unless}}>{{ this.title }}</span>
        </a>
      </li>
    {{/each}}
  </ul>
</nav>
